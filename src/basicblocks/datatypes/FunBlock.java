package basicblocks.datatypes;

public class FunBlock extends BBlock {
    // hat bisher keine besondere Funktionalitäten, wird nur bei Paramenterverwendung und bei return values interessant
    public FunBlock(int next) {
        super(next);
    }
    
}
